version: '3'

services:
  app_python:
    image: "{{ app_image }}:{{ app_tag }}"
    volumes: 
      - app-python-data:/app-python/cache
    restart: always
    ports:
      - "{{ app_port }}:{{ app_port }}"

volumes:
  app-python-data: